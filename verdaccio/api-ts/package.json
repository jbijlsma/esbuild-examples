{
  "name": "aws-sam-test",
  "version": "1.0.0",
  "description": "Check to see if we can do a manual esbuild by following this aws example:",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "./scripts/build.js",
    "start": "node ./dist/index.js",
    "uninstall": "rm -rf node_modules",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dnw/messages-package": "^0.0.4",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.10",
    "@types/express": "^4.17.14",
    "@types/node": "^18.7.23"
  }
}
